[package]
name = "intent_router"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
near-sdk = { version = "5", default-features = false, features = ["abi"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"


